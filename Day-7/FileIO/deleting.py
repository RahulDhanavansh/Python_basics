'''
using the OS module

Module(like a code library) is a file written by another programmer that generally has a function we can use

import os
os.remove(filename)

pip = pakage install for python
'''

import os;
os.remove("sample.txt")